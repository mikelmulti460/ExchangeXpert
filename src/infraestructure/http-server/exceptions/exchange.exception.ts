import {
  ExceptionFilter,
  Catch,
  ArgumentsHost,
  HttpStatus,
  Logger,
} from '@nestjs/common';
import { Response, Request } from 'express';
import { ExchangeApplicationError } from '../../../core/exchange/shared/error/ExchangeApplication.error';
import { ExchangeServiceError } from '../../../core/exchange/shared/error/ExchangeService.error';

@Catch(ExchangeApplicationError)
export class ExchangeFilter implements ExceptionFilter {
  catch(exception: ExchangeApplicationError, host: ArgumentsHost) {
    const ctx = host.switchToHttp();
    const response = ctx.getResponse<Response>();
    const request = ctx.getRequest<Request>();

    Logger.error(
      `ExchangeController (${request.method}) at {${request.path}} error: ${exception.message}`,
    );

    response.status(HttpStatus.BAD_REQUEST).json({
      status: HttpStatus.BAD_REQUEST,
      message: exception.message,
    });
  }
}

@Catch(ExchangeServiceError)
export class ExchangeServiceFilter implements ExceptionFilter {
  catch(exception: ExchangeServiceError, host: ArgumentsHost) {
    const ctx = host.switchToHttp();
    const response = ctx.getResponse<Response>();
    const request = ctx.getRequest<Request>();

    Logger.error(
      `ExchangeController (${request.method}) at {${request.path}} error: ${exception.message}`,
    );

    response.status(HttpStatus.BAD_REQUEST).json({
      status: HttpStatus.BAD_REQUEST,
      message: exception.message,
    });
  }
}
